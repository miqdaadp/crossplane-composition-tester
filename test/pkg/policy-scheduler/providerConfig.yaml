apiVersion: provider.aws.crossplane.io/v1beta1
kind: ProviderConfig
metadata:
  name: aws-provider
spec:
  # Specify AWS credentials
  credentialsSecretRef:
    name: aws-credentials
    namespace: crossplane-system
    key: credentials.json
